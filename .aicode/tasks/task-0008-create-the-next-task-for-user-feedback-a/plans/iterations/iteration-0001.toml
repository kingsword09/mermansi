schema_version = 1
task = "task-0008-create-the-next-task-for-user-feedback-a"
iteration = 1
loop_iteration = 1
created_at = "unix:1785516755"
objective = "Create the next Task for user feedback after completed Task 7: Ishikawa still falls back to source at 100-120 column terminals because its natural geometry is 143 columns. Implement bounded compressed slot layout and connected callouts so the Ishikawa fixture renders genuine geometry at width 120 in both English and Chinese without dropping labels or relationships. Add focused width, ASCII/Unicode, deterministic, and bounds tests. Preserve prior completed Task history, use one task-sourced Issue, review and commit the work, then complete the Task workflow."
open_issue_ids = ["task-0008-create-the-next-task-for-user-feedback-a#0001"]

[plan]
objective = "Create the next Task for user feedback after completed Task 7: Ishikawa still falls back to source at 100-120 column terminals because its natural geometry is 143 columns. Implement bounded compressed slot layout and connected callouts so the Ishikawa fixture renders genuine geometry at width 120 in both English and Chinese without dropping labels or relationships. Add focused width, ASCII/Unicode, deterministic, and bounds tests. Preserve prior completed Task history, use one task-sourced Issue, review and commit the work, then complete the Task workflow."

[[plan.steps]]
id = "step-1"
description = "Inspect `src/adapters/ishikawa.rs`, `tests/support_matrix.rs`, `tests/semantic_integrity.rs`, `tests/bounds.rs`, and Ishikawa fixture files to understand current Ishikawa layout parameters, column requirements, and fallback triggers."

[[plan.steps]]
id = "step-2"
description = "Modify `src/adapters/ishikawa.rs` to implement a bounded compressed slot layout and connected callout structure that fits Ishikawa diagrams into 120 display columns for both English and CJK text while preserving all categories, causes, and sub-causes."

[[plan.steps]]
id = "step-3"
description = "Add or update focused unit, integration, and width tests in `tests/semantic_integrity.rs`, `tests/bounds.rs`, and/or `tests/support_matrix.rs` validating Ishikawa rendering at 120 width in ASCII and Unicode for English and Chinese inputs."

[[plan.steps]]
id = "step-4"
description = "Run strict verification using `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, and `cargo test --all-targets --all-features` to confirm all assertions pass without regression."
