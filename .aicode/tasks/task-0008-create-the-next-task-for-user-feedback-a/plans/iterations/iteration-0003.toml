schema_version = 1
task = "task-0008-create-the-next-task-for-user-feedback-a"
iteration = 3
loop_iteration = 1
created_at = "unix:1785539659"
objective = "Create the next Task for user feedback after completed Task 7: Ishikawa still falls back to source at 100-120 column terminals because its natural geometry is 143 columns. Implement bounded compressed slot layout and connected callouts so the Ishikawa fixture renders genuine geometry at width 120 in both English and Chinese without dropping labels or relationships. Add focused width, ASCII/Unicode, deterministic, and bounds tests. Preserve prior completed Task history, use one task-sourced Issue, review and commit the work, then complete the Task workflow."
open_issue_ids = [
    "task-0008-create-the-next-task-for-user-feedback-a#0001",
    "task-0008-create-the-next-task-for-user-feedback-a#0002",
    "task-0008-create-the-next-task-for-user-feedback-a#0003",
]

[plan]
objective = "Create the next Task for user feedback after completed Task 7: Ishikawa still falls back to source at 100-120 column terminals because its natural geometry is 143 columns. Implement bounded compressed slot layout and connected callouts so the Ishikawa fixture renders genuine geometry at width 120 in both English and Chinese without dropping labels or relationships. Add focused width, ASCII/Unicode, deterministic, and bounds tests. Preserve prior completed Task history, use one task-sourced Issue, review and commit the work, then complete the Task workflow."

[[plan.steps]]
id = "step-1"
description = "Review the existing uncommitted changes in src/adapters/ishikawa.rs (compressed_slot_width, effect budget clamping, MIN/MAX_EFFECT_WIDTH, EFFECT_GAP_WIDTH) to confirm they already implement bounded compressed slot layout and connected callouts that fit within 120 display columns for both English and Chinese fixtures without dropping any labels or relationships."

[[plan.steps]]
id = "step-2"
description = "Verify that tests/semantic_integrity.rs::ishikawa_fixtures_render_bounded_geometry_at_100_and_120_columns and tests/bounds.rs::ishikawa_compression_respects_width_and_minimum_slot_bounds already cover width 100/120, ASCII/Unicode, determinism, label preservation (via connected callouts), and minimum-slot bounds. Add any missing edge-case assertion if a gap is found (e.g., Chinese callout connectivity, or verifying no label is silently dropped)."

[[plan.steps]]
id = "step-3"
description = "Run the full acceptance command: cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-targets --all-features to confirm zero regressions and that all Ishikawa-focused tests pass."
