schema_version = 1
task = "task-0005-replace-the-structured-text-only-pie-rad"
iteration = 12
loop_iteration = 1
created_at = "unix:1785502098"
objective = "Replace the structured-text-only Pie, Radar, QuadrantChart, and Venn outputs with genuine deterministic terminal chart geometry. Implement mermansi-native bounded Canvas geometry without modifying or forking merman-core, merman-ascii, Selkie, or any parser. Pie must draw a closed circular/radial chart with visible sector boundaries or proportional sector fill derived from every finite nonnegative section value, plus a compact legend preserving every label, value, percentage, title, and showData semantics; a bar/table alone is not acceptable. Radar must draw a shared center, radial axis spokes, graticule rings or polygons, and connected plotted curve vertices normalized from configured min/max while preserving every axis label, curve label/value, ticks, graticule choice, showLegend, and title. QuadrantChart must draw a closed Cartesian plotting area with midpoint cross-axes, all four quadrant labels in their actual regions, endpoint axis labels, and every point placed from its normalized x/y coordinates with deterministic collision-safe markers/callouts; preserve class and style metadata without pretending terminal color is geometry. Venn must draw closed overlapping set outlines for the semantic singleton sets, place intersection/subset and text labels in or connect them to their associated regions, preserve sizes, labels, styles, and arbitrary parsed subsets, and remain genuine geometry rather than a Subsets/Text Nodes list. For every family preserve Complete-mode canonical models, Concise-mode chart geometry with no structured-text fallback, ASCII and Unicode geometry, Chinese/CJK display widths, ANSI-independent geometry, determinism, control sanitization, and typed width/height/cell/byte/entity limits before allocations. Add strong tests for shape closure, proportional/coordinate placement, axis/spoke/curve connectivity, overlap identity, collision handling, empty/zero/invalid constructed models, bilingual fixtures, narrow widths, ASCII, determinism, and bounds. Update AGENTS.md, README.md, and docs/SUPPORT_MATRIX.md honestly. Apply KISS, YAGNI, DRY, and SOLID: reuse only minimal chart primitives that are genuinely shared."
open_issue_ids = []

[plan]
objective = "Replace the structured-text-only Pie, Radar, QuadrantChart, and Venn outputs with genuine deterministic terminal chart geometry. Implement mermansi-native bounded Canvas geometry without modifying or forking merman-core, merman-ascii, Selkie, or any parser. Pie must draw a closed circular/radial chart with visible sector boundaries or proportional sector fill derived from every finite nonnegative section value, plus a compact legend preserving every label, value, percentage, title, and showData semantics; a bar/table alone is not acceptable. Radar must draw a shared center, radial axis spokes, graticule rings or polygons, and connected plotted curve vertices normalized from configured min/max while preserving every axis label, curve label/value, ticks, graticule choice, showLegend, and title. QuadrantChart must draw a closed Cartesian plotting area with midpoint cross-axes, all four quadrant labels in their actual regions, endpoint axis labels, and every point placed from its normalized x/y coordinates with deterministic collision-safe markers/callouts; preserve class and style metadata without pretending terminal color is geometry. Venn must draw closed overlapping set outlines for the semantic singleton sets, place intersection/subset and text labels in or connect them to their associated regions, preserve sizes, labels, styles, and arbitrary parsed subsets, and remain genuine geometry rather than a Subsets/Text Nodes list. For every family preserve Complete-mode canonical models, Concise-mode chart geometry with no structured-text fallback, ASCII and Unicode geometry, Chinese/CJK display widths, ANSI-independent geometry, determinism, control sanitization, and typed width/height/cell/byte/entity limits before allocations. Add strong tests for shape closure, proportional/coordinate placement, axis/spoke/curve connectivity, overlap identity, collision handling, empty/zero/invalid constructed models, bilingual fixtures, narrow widths, ASCII, determinism, and bounds. Update AGENTS.md, README.md, and docs/SUPPORT_MATRIX.md honestly. Apply KISS, YAGNI, DRY, and SOLID: reuse only minimal chart primitives that are genuinely shared."

[[plan.steps]]
id = "step-1"
description = "Run the full test suite (lib, semantic_integrity, bounds, support_matrix, cli) and confirm all 206 tests pass with zero failures."

[[plan.steps]]
id = "step-2"
description = "Run cargo clippy --all-targets and cargo fmt --check to confirm zero warnings and correct formatting."

[[plan.steps]]
id = "step-3"
description = "Audit Pie output for closed circular outline, proportional sector fill from every finite nonnegative section value, and compact legend preserving labels/values/percentages/title/showData semantics."

[[plan.steps]]
id = "step-4"
description = "Audit Radar output for shared center, radial spokes, graticule rings/polygons, connected plotted curve vertices normalized from min/max, and preservation of axis labels, curve labels/values, ticks, graticule choice, showLegend, and title."

[[plan.steps]]
id = "step-5"
description = "Audit QuadrantChart output for closed Cartesian plotting area with midpoint cross-axes, all four quadrant labels in correct regions, endpoint axis labels, and every point placed from normalized coordinates with collision-safe markers/callouts."

[[plan.steps]]
id = "step-6"
description = "Audit Venn output for closed overlapping set outlines, intersection/subset and text labels placed in or connected to associated regions, and preservation of sizes/labels/styles/arbitrary subsets."

[[plan.steps]]
id = "step-7"
description = "Confirm shared chart_primitives.rs genuinely serves multiple adapters (Pie, Radar, QuadrantChart, Venn) with minimal bounded primitives and KISS/DRY compliance."

[[plan.steps]]
id = "step-8"
description = "Verify ANSI roles add color only and never alter geometry by comparing plain vs ANSI output substrings."

[[plan.steps]]
id = "step-9"
description = "Verify CJK display-width correctness in chart labels and that Chinese fixtures render correctly."

[[plan.steps]]
id = "step-10"
description = "Verify determinism (byte-identical output on repeat render) for all four families in both Complete and Concise modes."

[[plan.steps]]
id = "step-11"
description = "Verify typed width/height/cell/byte/entity limits are enforced before allocations for all four families."

[[plan.steps]]
id = "step-12"
description = "Confirm AGENTS.md, README.md, and docs/SUPPORT_MATRIX.md accurately and honestly document the current chart-geometry state without overclaiming browser geometric parity."
