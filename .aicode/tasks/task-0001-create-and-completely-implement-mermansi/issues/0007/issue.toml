schema_version = 2
id = "task-0001-create-and-completely-implement-mermansi#0007"
number = 7
title = "Self-loops and parallel Flowchart edges render as semantic JSON rather than terminal geometry"
status = "open"
severity = "P1"
category = "terminal-geometry"
fingerprint = "sha256:58d9108f9543de9268e20dd1ad23d17c366ad26b732b63be224dc4eb5b37484c"
created_at = "unix:1785338735"
updated_at = "unix:1785338735"

[source]
kind = "review"
review = "../../reviews/iteration-0016.json"

[location]
path = "src/adapters/flowchart.rs"
line = 12

[problem]
description = "Self-loop and parallel Flowchart edges fall back to a semantic JSON representation instead of terminal-native geometry. The existing tests only check for edge labels and do not verify that self-loops or parallel edges produce distinct geometric routing, so the deficiency is masked."
impact = "Rule 5 (terminal-native geometry where meaningful) and Rule 4 are violated; self-loops and parallel edges are not rendered as distinct geometric edges."

[acceptance]
description = "Render self-loops and parallel edges with distinct, visible terminal geometry (not a JSON fallback). Extend tests to assert that the geometric output contains the expected self-loop and parallel-edge routing shapes, not just label text."
command = "cargo test --test semantic_integrity"
