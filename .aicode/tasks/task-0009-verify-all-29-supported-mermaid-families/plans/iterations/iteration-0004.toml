schema_version = 1
task = "task-0009-verify-all-29-supported-mermaid-families"
iteration = 4
loop_iteration = 1
created_at = "unix:1785550564"
objective = "Verify all 29 supported Mermaid families across at least 58 English and Chinese fixtures, ASCII and Unicode charsets, and maximum widths 100 and 120 (232 render combinations). Every combination must render deterministic, bounded, nonempty terminal-native geometry in concise output, with no structured-text or semantic-model fallback and with parsed labels, entities, relationships, hierarchy, and chart values preserved. Add a single executable support-matrix gate in tests/support_matrix.rs using shared assertions instead of family exceptions; fix every renderer defect it exposes without weakening fixtures or assertions. Run all required checks, perform immutable Review, resolve every Issue, commit each reviewed unit, and complete the new Task. Preserve all completed Task history."
open_issue_ids = ["task-0009-verify-all-29-supported-mermaid-families#0001"]

[plan]
objective = "Verify all 29 supported Mermaid families across at least 58 English and Chinese fixtures, ASCII and Unicode charsets, and maximum widths 100 and 120 (232 render combinations). Every combination must render deterministic, bounded, nonempty terminal-native geometry in concise output, with no structured-text or semantic-model fallback and with parsed labels, entities, relationships, hierarchy, and chart values preserved. Add a single executable support-matrix gate in tests/support_matrix.rs using shared assertions instead of family exceptions; fix every renderer defect it exposes without weakening fixtures or assertions. Run all required checks, perform immutable Review, resolve every Issue, commit each reviewed unit, and complete the new Task. Preserve all completed Task history."

[[plan.steps]]
id = "step-1"
description = "Verify fixture coverage: confirm 29 families each have .en.mmd and .zh.mmd fixtures (58 total) in tests/fixtures/. The existing all_expected_families_have_en_and_zh_fixtures test already enforces this."

[[plan.steps]]
id = "step-2"
description = "Verify the support-matrix gate tests/support_matrix.rs uses shared assertions (assert_concise_terminal_geometry, assert_structured_model_round_trips) with no per-family exception branches, and that every_fixture_renders_terminal_geometry_in_the_supported_width_matrix exercises all 58 fixtures × 2 charsets × 2 widths = 232 combinations with a rendered_combinations count assertion."

[[plan.steps]]
id = "step-3"
description = "Run the acceptance command cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-targets --all-features. If any check fails, fix the exposed renderer defect without weakening fixtures or assertions, then re-run until all pass."

[[plan.steps]]
id = "step-4"
description = "Report changed:true (the active Issue still owns the work) with a summary that all 232 combinations render deterministic, bounded, nonempty terminal geometry with no fallback, and that the support-matrix gate enforces this uniformly."
