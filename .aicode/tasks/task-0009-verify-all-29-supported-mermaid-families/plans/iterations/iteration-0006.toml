schema_version = 1
task = "task-0009-verify-all-29-supported-mermaid-families"
iteration = 6
loop_iteration = 1
created_at = "unix:1785554325"
objective = "Verify all 29 supported Mermaid families across at least 58 English and Chinese fixtures, ASCII and Unicode charsets, and maximum widths 100 and 120 (232 render combinations). Every combination must render deterministic, bounded, nonempty terminal-native geometry in concise output, with no structured-text or semantic-model fallback and with parsed labels, entities, relationships, hierarchy, and chart values preserved. Add a single executable support-matrix gate in tests/support_matrix.rs using shared assertions instead of family exceptions; fix every renderer defect it exposes without weakening fixtures or assertions. Run all required checks, perform immutable Review, resolve every Issue, commit each reviewed unit, and complete the new Task. Preserve all completed Task history."
open_issue_ids = ["task-0009-verify-all-29-supported-mermaid-families#0001"]

[plan]
objective = "Verify all 29 supported Mermaid families across at least 58 English and Chinese fixtures, ASCII and Unicode charsets, and maximum widths 100 and 120 (232 render combinations). Every combination must render deterministic, bounded, nonempty terminal-native geometry in concise output, with no structured-text or semantic-model fallback and with parsed labels, entities, relationships, hierarchy, and chart values preserved. Add a single executable support-matrix gate in tests/support_matrix.rs using shared assertions instead of family exceptions; fix every renderer defect it exposes without weakening fixtures or assertions. Run all required checks, perform immutable Review, resolve every Issue, commit each reviewed unit, and complete the new Task. Preserve all completed Task history."

[[plan.steps]]
id = "step-1"
description = "Confirm fixture coverage: verify all 29 EXPECTED_FAMILIES each have an .en.mmd and .zh.mmd fixture (58 total) in tests/fixtures/, enforced by the all_expected_families_have_en_and_zh_fixtures test. Confirm upstream_render_parser_inventory_is_fully_accounted_for still matches the pinned merman-core render-parser set (33 IDs)."

[[plan.steps]]
id = "step-2"
description = "Confirm the support-matrix gate tests/support_matrix.rs uses only shared assertions (assert_concise_terminal_geometry, assert_structured_model_round_trips) with zero per-family exception branches, and that every_fixture_renders_terminal_geometry_in_the_supported_width_matrix iterates every fixture × {ASCII, Unicode} × {width 100, 120} = 232 combinations with an exact rendered_combinations == total*2*2 assertion enforcing nonempty, bounded, deterministic, terminal-geometry-only concise output that preserves quoted labels and round-trips the semantic model."

[[plan.steps]]
id = "step-3"
description = "Run the acceptance command cargo fmt --check && cargo clippy --all-targets --all-features -- -D warnings && cargo test --all-targets --all-features. If any check or any of the 232 combinations fails, fix the exposed renderer defect in the relevant src/adapters/*.rs adapter (without weakening fixtures, assertions, or adding family exceptions) and re-run until all pass."

[[plan.steps]]
id = "step-4"
description = "Report changed:true because the active Issue still owns uncommitted repository changes (tests/support_matrix.rs, src/adapters/mod.rs, src/adapters/xychart.rs, README.md, docs/SUPPORT_MATRIX.md), with a summary that all 232 combinations render deterministic, bounded, nonempty terminal-native geometry with no fallback and parsed labels/entities/relationships/hierarchy/chart values preserved."
